# Real-Time Notification System - Visual Architecture

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         FIREBASE CLOUD                               │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    FIRESTORE DATABASE                         │   │
│  │                                                               │   │
│  │  📦 notifications (Admin Dashboard)                          │   │
│  │  ├─ notif_001                                                │   │
│  │  │  ├─ title: "O- Blood Needed"                             │   │
│  │  │  ├─ type: "urgent"                                        │   │
│  │  │  └─ targetAudience: "donors"                             │   │
│  │                                                               │   │
│  │  📦 userNotifications (User Inbox)                           │   │
│  │  ├─ user_notif_001                                           │   │
│  │  │  ├─ userId: "user_123"                                    │   │
│  │  │  ├─ title: "O- Blood Needed"                             │   │
│  │  │  ├─ read: false                                           │   │
│  │  │  └─ receivedAt: Timestamp                                │   │
│  │                                                               │   │
│  │  📦 users                                                     │   │
│  │  ├─ user_123                                                 │   │
│  │  │  ├─ role: "donor"                                         │   │
│  │  │  └─ pushToken: "Expo[...]"                               │   │
│  │                                                               │   │
│  │  📦 admins                                                    │   │
│  │  └─ admin_001                                                │   │
│  │     ├─ email: "admin@hospital.com"                          │   │
│  │     └─ role: "admin"                                         │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                       │
│                    🔐 Security Rules                                  │
│                    📊 Indexes                                         │
│                    ⚡ Real-time Listeners                             │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
                    ▼                             ▼
        ┌───────────────────────┐     ┌───────────────────────┐
        │   ADMIN DEVICE        │     │   USER DEVICE         │
        │                       │     │                       │
        │  📱 Admin Dashboard   │     │  📱 User App          │
        │                       │     │                       │
        │  ┌─────────────────┐ │     │  ┌─────────────────┐ │
        │  │ Send Notification│ │     │  │ Notifications   │ │
        │  │                 │ │     │  │                 │ │
        │  │ [Title____]     │ │     │  │ 🔴 O- Blood... │ │
        │  │ [Message___]    │ │     │  │    5m ago       │ │
        │  │ Type: [Urgent]  │ │     │  │                 │ │
        │  │ To: [Donors]    │ │     │  │ 🔵 Event...     │ │
        │  │                 │ │     │  │    1h ago       │ │
        │  │ [Send Button]   │ │     │  └─────────────────┘ │
        │  └─────────────────┘ │     │                       │
        │           │           │     │  Real-time Listener   │
        └───────────┼───────────┘     │  ⚡ Auto-updates     │
                    │                 └───────────────────────┘
                    │                             ▲
                    └─────────────────────────────┘
                         INSTANT DELIVERY
                              < 1 sec
```

## 🔄 Notification Flow Diagram

```
ADMIN SENDS NOTIFICATION:
═══════════════════════════

1️⃣  Admin fills form
    ┌─────────────────┐
    │ Title: O- Blood │
    │ Message: Urgent │
    │ Type: Critical  │
    │ To: All Donors  │
    └────────┬────────┘
             │
             ▼
2️⃣  Click "Send"
    ┌─────────────────┐
    │   [Send] 🚀     │
    └────────┬────────┘
             │
             ▼
3️⃣  sendNotification()
    ┌─────────────────────────────┐
    │ Create notification doc     │
    │ Get target users (donors)   │
    │ Create userNotification docs│
    └────────┬────────────────────┘
             │
             ▼
4️⃣  Firestore writes
    ┌─────────────────────────────┐
    │ notifications/notif_001     │
    ├─────────────────────────────┤
    │ userNotifications/user1     │
    │ userNotifications/user2     │
    │ userNotifications/user3     │
    └────────┬────────────────────┘
             │
             ▼
5️⃣  Real-time triggers ⚡
    ┌─────────────────────────────┐
    │ Firestore onSnapshot        │
    │ listeners activated         │
    └────────┬────────────────────┘
             │
        ┌────┴─────┬──────┬────────┐
        ▼          ▼      ▼        ▼
    User 1     User 2  User 3  User N
    📱 ding!   📱 ding! 📱 ding! 📱 ding!
    
    All receive INSTANTLY (< 1 second!)


USER RECEIVES NOTIFICATION:
════════════════════════════

1️⃣  App starts
    ┌─────────────────┐
    │ User opens app  │
    └────────┬────────┘
             │
             ▼
2️⃣  Setup listener
    ┌───────────────────────────────┐
    │ useEffect(() => {             │
    │   listenToUserNotifications() │
    │   return () => unsubscribe()  │
    │ }, [])                        │
    └────────┬──────────────────────┘
             │
             ▼
3️⃣  Listener active 👂
    ┌──────────────────────────┐
    │ Waiting for new data...  │
    └────────┬─────────────────┘
             │
             ▼
4️⃣  New notification arrives! ⚡
    ┌──────────────────────────┐
    │ onSnapshot triggered     │
    └────────┬─────────────────┘
             │
             ▼
5️⃣  Update UI instantly
    ┌──────────────────────────┐
    │ setNotifications(data)   │
    │ UI re-renders            │
    └────────┬─────────────────┘
             │
             ▼
6️⃣  User sees notification 👀
    ┌──────────────────────────┐
    │ 🔴 O- Blood Needed       │
    │    Urgent! City Hospital │
    │    just now              │
    └──────────────────────────┘
```

## 📊 Data Flow Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                      ADMIN INTERFACE                          │
│  ┌────────────────────────────────────────────────────┐      │
│  │  Admin Notifications Screen                        │      │
│  │  - Send notification form                          │      │
│  │  - Real-time dashboard                             │      │
│  │  - Delivery statistics                             │      │
│  └────────┬───────────────────────────────────────────┘      │
└───────────┼──────────────────────────────────────────────────┘
            │
            ▼
    notificationService.ts
    ┌────────────────────────────────────┐
    │ sendNotification()                 │
    │ ├─ Create main notification doc   │
    │ ├─ Get target user IDs             │
    │ ├─ Create individual user docs    │
    │ └─ Batch write to Firestore       │
    └────────┬───────────────────────────┘
             │
             ▼
    ┌────────────────────────────────────┐
    │         FIRESTORE CLOUD            │
    │  ┌──────────────────────────────┐ │
    │  │ notifications collection     │ │
    │  │ (Admin stats & history)      │ │
    │  └──────────────────────────────┘ │
    │  ┌──────────────────────────────┐ │
    │  │ userNotifications collection │ │
    │  │ (Individual user inboxes)    │ │
    │  └──────────────────────────────┘ │
    └────────┬───────────────────────────┘
             │
             │ ⚡ Real-time Sync
             │
        ┌────┴─────────────────────────┐
        ▼                              ▼
┌───────────────────┐        ┌────────────────────┐
│  ADMIN DEVICE     │        │  USER DEVICE       │
│  ┌─────────────┐  │        │  ┌──────────────┐  │
│  │ Dashboard   │  │        │  │ Notification │  │
│  │ with stats  │  │        │  │ List         │  │
│  │             │  │        │  │              │  │
│  │ Total: 156  │  │        │  │ 🔴 New      │  │
│  │ Read: 98    │  │        │  │ 🔵 Read     │  │
│  │ Rate: 63%   │  │        │  │ 🔵 Read     │  │
│  └─────────────┘  │        │  └──────────────┘  │
│                   │        │                    │
│  Listener Active  │        │  Listener Active   │
│  ⚡ Auto-updates  │        │  ⚡ Auto-updates   │
└───────────────────┘        └────────────────────┘
```

## 🔐 Security Architecture

```
┌────────────────────────────────────────────────────────┐
│              FIREBASE AUTHENTICATION                    │
│  ┌──────────────────────────────────────────────┐     │
│  │  User Authentication Layer                   │     │
│  │  - Email/Password                            │     │
│  │  - Social Auth (Google, etc.)                │     │
│  │  - Token management                          │     │
│  └────────┬─────────────────────────────────────┘     │
└───────────┼────────────────────────────────────────────┘
            │
            ▼
┌────────────────────────────────────────────────────────┐
│           FIRESTORE SECURITY RULES                      │
│  ┌──────────────────────────────────────────────┐     │
│  │  Rule: Only admins can send                  │     │
│  │  ✅ if exists(/admins/{userId})              │     │
│  │  ❌ else deny                                 │     │
│  └──────────────────────────────────────────────┘     │
│  ┌──────────────────────────────────────────────┐     │
│  │  Rule: Users read own notifications          │     │
│  │  ✅ if resource.data.userId == request.uid   │     │
│  │  ❌ else deny                                 │     │
│  └──────────────────────────────────────────────┘     │
│  ┌──────────────────────────────────────────────┐     │
│  │  Rule: Users update only read status         │     │
│  │  ✅ if changed fields = ['read', 'readAt']   │     │
│  │  ❌ else deny                                 │     │
│  └──────────────────────────────────────────────┘     │
└────────────────────────────────────────────────────────┘
            │
            ▼
┌────────────────────────────────────────────────────────┐
│              DATA ACCESS LAYER                          │
│  ┌──────────────────────────────────────────────┐     │
│  │  Admin Access:                               │     │
│  │  ✅ Read all notifications                   │     │
│  │  ✅ Write new notifications                  │     │
│  │  ✅ View statistics                          │     │
│  └──────────────────────────────────────────────┘     │
│  ┌──────────────────────────────────────────────┐     │
│  │  User Access:                                │     │
│  │  ✅ Read own notifications only              │     │
│  │  ✅ Update own read status                   │     │
│  │  ❌ Cannot read others' notifications        │     │
│  │  ❌ Cannot send notifications                │     │
│  └──────────────────────────────────────────────┘     │
└────────────────────────────────────────────────────────┘
```

## ⚡ Real-Time Listener Lifecycle

```
COMPONENT MOUNT:
═════════════════

useEffect(() => {
  │
  ├─ Setup listener
  │  ┌────────────────────────────────┐
  │  │ onSnapshot(query, callback)    │
  │  │ - Query: userId + orderBy date │
  │  │ - Limit: 50 notifications      │
  │  └────────────────────────────────┘
  │
  ├─ Listener ACTIVE 👂
  │  ┌────────────────────────────────┐
  │  │ Waiting for changes...         │
  │  │ Connected to Firestore         │
  │  └────────────────────────────────┘
  │
  ├─ Data changes detected! ⚡
  │  ┌────────────────────────────────┐
  │  │ New notification added         │
  │  │ → Callback triggered           │
  │  │ → Update state                 │
  │  │ → UI re-renders                │
  │  └────────────────────────────────┘
  │
  ├─ More changes... ⚡
  │  ┌────────────────────────────────┐
  │  │ User marks as read             │
  │  │ → Callback triggered           │
  │  │ → Update state                 │
  │  │ → UI re-renders                │
  │  └────────────────────────────────┘
  │
  return () => {
    └─ CLEANUP (Component unmount)
       ┌────────────────────────────────┐
       │ unsubscribe()                  │
       │ - Detach listener              │
       │ - Free resources               │
       │ - Prevent memory leak          │
       └────────────────────────────────┘
  }
}, []);  // Empty deps = run once


TIMELINE:
═════════

0ms    │ Component mounts
       │ ↓
10ms   │ Listener attached
       │ ↓
       │ ... waiting for changes ...
       │
2000ms │ Admin sends notification
       │ ↓
2050ms │ Firestore processes
       │ ↓
2100ms │ Listener callback triggered ⚡
       │ ↓
2110ms │ State updated
       │ ↓
2120ms │ UI re-renders
       │ ↓
       │ USER SEES NOTIFICATION! 👀
       │ (Total: ~120ms from send to display)
       │
       │ ... more changes ...
       │
10000ms│ User navigates away
       │ ↓
10010ms│ Component unmounts
       │ ↓
10020ms│ Cleanup runs
       │ ↓
10030ms│ Listener detached
       │
       │ ✅ Clean exit, no memory leak
```

## 🎯 Targeting System

```
TARGET AUDIENCE SELECTION:
══════════════════════════

Admin selects: "donors"
       │
       ▼
getTargetUserIds("donors")
       │
       ├─ Query Firestore
       │  ┌──────────────────────────────┐
       │  │ collection('users')          │
       │  │ .where('role', '==', 'donor')│
       │  └────────┬─────────────────────┘
       │           │
       │           ▼
       │  ┌──────────────────────────────┐
       │  │ Returns:                     │
       │  │ ['user1', 'user2', 'user3']  │
       │  └────────┬─────────────────────┘
       │           │
       └───────────┘
       │
       ▼
createUserNotifications(ids)
       │
       ├─ For each user ID:
       │  ┌──────────────────────────────┐
       │  │ userNotifications/doc_001    │
       │  │ ├─ userId: 'user1'           │
       │  │ ├─ title: '...'              │
       │  │ └─ read: false               │
       │  └──────────────────────────────┘
       │
       ├─ Batch write (parallel)
       │  ┌──────────────────────────────┐
       │  │ Promise.all([               │
       │  │   writeDoc(user1),          │
       │  │   writeDoc(user2),          │
       │  │   writeDoc(user3),          │
       │  │ ])                          │
       │  └────────┬─────────────────────┘
       │           │
       │           ▼
       └──── ✅ All users notified!


TARGET OPTIONS:
═══════════════

┌────────────────────────────────────────────────┐
│ "all"                                          │
│ └─> Query: all users                          │
│     Result: Everyone gets notification        │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│ "donors"                                       │
│ └─> Query: role == 'donor'                    │
│     Result: Only donors get notification       │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│ "recipients"                                   │
│ └─> Query: role == 'recipient'                │
│     Result: Only recipients get notification   │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│ "specific"                                     │
│ └─> Uses: targetUserIds array                 │
│     Result: Only specified users get it        │
└────────────────────────────────────────────────┘
```

## 📈 Scalability

```
PERFORMANCE AT SCALE:
═════════════════════

Users:           10        100       1,000     10,000
                 │          │          │          │
Send Time:      50ms      100ms     500ms      2sec
                 │          │          │          │
Delivery:     <100ms    <200ms    <500ms      <2sec
                 │          │          │          │
DB Writes:       10        100      1,000     10,000
                 │          │          │          │
Cost/Send:     $0.001    $0.01     $0.10      $1.00
                 │          │          │          │
Status:         ✅         ✅         ✅         ✅


OPTIMIZATION STRATEGIES:
════════════════════════

1. Batch Writes
   ┌─────────────────────────────────┐
   │ Instead of:                     │
   │ for (user of users) {           │
   │   await writeDoc(user)  // Slow │
   │ }                               │
   │                                 │
   │ Use:                            │
   │ await Promise.all(              │
   │   users.map(u => writeDoc(u))   │
   │ ) // Fast & Parallel            │
   └─────────────────────────────────┘

2. Query Limits
   ┌─────────────────────────────────┐
   │ query(..., limit(50))           │
   │ - Only load what's needed       │
   │ - Reduces read costs            │
   │ - Faster initial load           │
   └─────────────────────────────────┘

3. Indexed Queries
   ┌─────────────────────────────────┐
   │ Composite indexes:              │
   │ - userId + receivedAt           │
   │ - userId + read                 │
   │ Result: Millisecond queries     │
   └─────────────────────────────────┘
```

This visual architecture helps understand how all the pieces fit together! 🎨
